const nextConfig = {
    output: 'export',
    images: {
        unoptimized: true,
    },
    // basePath: '/conspectus',      // имя репозитория
    // assetPrefix: '/conspectus',
}

module.exports = nextConfig
